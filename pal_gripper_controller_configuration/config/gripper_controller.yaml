${EE_SUFFIX}_controller:
  ros__parameters:
    type: joint_trajectory_controller/JointTrajectoryController
    joints:
      - ${EE_SUFFIX}_right_finger_joint
      - ${EE_SUFFIX}_left_finger_joint
    command_interfaces:
      - position
    state_interfaces:
      - position

    constraints:
      goal_time: 0.6
      stopped_velocity_tolerance: 5.0
      ${EE_SUFFIX}_right_finger_joint:
        goal: 0.02
      ${EE_SUFFIX}_left_finger_joint:
        goal: 0.02
